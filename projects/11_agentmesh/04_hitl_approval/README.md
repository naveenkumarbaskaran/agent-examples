# 04_hitl_approval

Human-in-the-loop via agentmesh request/reply. Agent requests approval for a high-value
transfer. Mock human reviewer receives the event and responds. Shows the full approval loop
with timeout fallback.

## Install & Run
```bash
pip install agentmesh-bus
python main.py
```
