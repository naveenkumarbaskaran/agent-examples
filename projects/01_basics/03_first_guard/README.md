# 03_first_guard

Safety guardrails with AgentGuard across all three rule categories: input validation (prompt injection, jailbreak, token limits), output redaction (PII, credentials, internal URLs), and tool governance (allowlist + blocklist).

Run: `python main.py`
