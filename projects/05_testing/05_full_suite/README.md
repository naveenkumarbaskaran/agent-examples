# 05_full_suite

Full EvalSuite combining 11 tests across all four test types: GoldenTest (output assertions), AdversarialTest (injection blocking), PolicyTest (allow/block paths), and RegressionTest (baseline comparison). Asserts overall pass rate >= 70%.

Run: `python main.py`
