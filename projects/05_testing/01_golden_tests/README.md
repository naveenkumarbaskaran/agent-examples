# 01_golden_tests

Five GoldenTest cases that validate expected agent outputs: substring matching, latency bounds, and pass-through (no assertions). Uses MockAgent with pre-programmed responses.

Run: `python main.py`
