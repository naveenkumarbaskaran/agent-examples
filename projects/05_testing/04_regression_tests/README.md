# 04_regression_tests

Demonstrates RegressionTest with baseline comparison: one test passes (matches baseline), one intentionally fails (changed output detected), and two capture with no baseline. Shows how regression tests catch output drift.

Run: `python main.py`
