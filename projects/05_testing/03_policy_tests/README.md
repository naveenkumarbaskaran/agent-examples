# 03_policy_tests

PolicyTest cases testing allowed, blocked, and degraded evaluation paths using MockPolicyEngine. Covers allow_all=True (pass-through) and allow_all=False (block/degrade) scenarios.

Run: `python main.py`
