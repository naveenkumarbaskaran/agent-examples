# 01_snapshot

Writes mock agentplane audit JSONL entries, reads them with agentobserve Collector, and prints a full Snapshot with per-agent stats (evals, blocks), per-policy stats, and overall block rate.

Run: `python main.py`
