# 03_audit_trail

Uses agentplane AuditTrail to write 20 structured audit entries (mix of allow/block/degrade), reads them back from the JSONL file, and shows filtering by status with a breakdown bar chart.

Run: `python main.py`
