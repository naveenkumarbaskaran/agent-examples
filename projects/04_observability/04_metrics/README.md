# 04_metrics

Non-blocking observability using MetricsRule (per-agent call counters) and CostTrackingRule (USD totals per tenant). Runs 10 evaluations across two tenants and prints cost breakdown and metric counts.

Run: `python main.py`
