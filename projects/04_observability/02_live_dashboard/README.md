# 02_live_dashboard

Combines a live PolicyEngine with agentobserve. Runs five tool calls (mix of allowed and blocked), then prints a dashboard snapshot showing agent status, evaluation counts, and block rate.

Run: `python main.py`
