# 04_conflict_resolution

Two conflicting policies resolved two ways: MOST_RESTRICTIVE (block always wins regardless of priority) and PRIORITY (highest-priority policy verdict wins). Shows a deny policy overriding an allow policy and vice versa.

Run: `python main.py`
