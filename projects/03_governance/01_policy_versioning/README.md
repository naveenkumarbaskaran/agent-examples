# 01_policy_versioning

Policy lifecycle management with agentplane VersionManager. Publishes three policy versions with progressively stricter rules, shows version diffs (added/removed rules), and demonstrates rollback to v1.

Run: `python main.py`
